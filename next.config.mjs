/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        // if for production this will be under env instead
        domains: ["rickandmortyapi.com"],
    },
};

export default nextConfig;
